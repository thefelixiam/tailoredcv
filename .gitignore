# dependencies
node_modules/

# next
.next/
out/

# env & secrets (never commit)
.env
.env*.local
*.pem
*.key

# sqlite data
data/*.db
data/*.db-*

# browser binaries (installed via `npx puppeteer browsers install chrome`)
# (kept in user cache, nothing to ignore locally)

# AI coding assistants & agent instructions (local-only, never commit)
.opencode/
AGENTS.md

# test / debug artifacts
test-results/
*.test.pdf
tsconfig.tsbuildinfo
